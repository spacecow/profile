Feature:
Background:
Given a project exists
And a page exists with project: that project
And a section exists with page: that page
And a picture exists with section: that section

Scenario: Change picture attributes
When I go to that project's page's edit page
