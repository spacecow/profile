<%= semantic_form_for [@project,@page] do |f| %>
  <%= f.input :name %>
  <%= f.semantic_fields_for :sections do |ref| %>
    <%= render 'section_fields', :f => ref %>
  <% end %>
  <%= f.submit %>
<% end %>
