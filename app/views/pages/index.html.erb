<% title "Pages" %>

<table>
  <tr>
    <th>String</th>
    <th>Project</th>
  </tr>
  <% for page in @pages %>
    <tr>
      <td><%= page.string %></td>
      <td><%= page.project_id %></td>
      <td><%= link_to "Show", page %></td>
      <td><%= link_to "Edit", edit_page_path(page) %></td>
      <td><%= link_to "Destroy", page, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Page", new_page_path(page) %></p>
